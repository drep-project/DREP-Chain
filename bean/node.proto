syntax = "proto3";

package bean;

import "mycrypto/key.proto";

message peer_info {
    mycrypto.point pk = 1;
    string ip = 2;
    int32 port = 3;
}

message peer_info_list {
    repeated peer_info list = 1;
}

message miner_info {
    mycrypto.point pk = 1;
    peer_info peer = 2;
}