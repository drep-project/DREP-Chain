syntax = "proto3";

package bean;

import "crypto/key.proto";

message newcomer {
    crypto.point pk = 1;
    string ip = 2;
    int32 port = 3;
}

message listOfPeer {
    repeated newcomer list = 1;
}